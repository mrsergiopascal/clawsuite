# OpenClaw Studio → Gateway connection
#
# The Studio server connects to the OpenClaw Gateway via WebSocket.
# Keep secrets here (never in the browser).

CLAWDBOT_GATEWAY_URL=ws://127.0.0.1:18789
# Recommended auth method:
CLAWDBOT_GATEWAY_TOKEN=
# Alternative:
# CLAWDBOT_GATEWAY_PASSWORD=

# Allow access from non-localhost (Tailscale, LAN, etc.)
# Comma-separated hostnames. Also binds server to 0.0.0.0.
# Example: CLAWSUITE_ALLOWED_HOSTS=my-server.tail1234.ts.net,192.168.1.50
# CLAWSUITE_ALLOWED_HOSTS=

# Password-protect ClawSuite (leave empty for no auth)
# CLAWSUITE_PASSWORD=
